# pangeo-forge-aws-batch

```
nvm use 20
npm install -g aws-cdk
cd cdk
virtualenv -p python3 venv
source venv/bin/activate
python3 -m pip install -r requirements.txt
# run `cdk boostrap` if your AWS account and region are not bootstrapped.
cdk deploy
```
